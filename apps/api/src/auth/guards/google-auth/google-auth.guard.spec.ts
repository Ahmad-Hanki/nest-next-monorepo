import { GoogleAuthGuard } from './google-auth.guard';

describe('GoogleAuthGuardsTsGuard', () => {
  it('should be defined', () => {
    expect(new GoogleAuthGuard()).toBeDefined();
  });
});
