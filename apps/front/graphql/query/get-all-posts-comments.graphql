query Comments($postId: Int!, $page: Int) {
  comments(postId: $postId, page: $page) {
    pagination {
      ...PaginationMeta
    }
    items {
      ...Comment
      author {
        name
        avatar
        role
        id
      }
    }
  }
}
