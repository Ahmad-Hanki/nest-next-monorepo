query Post($postId: Int!) {
  post(id: $postId) {
    ...Post
    likeCount
    isLikedByCurrentUser
    author {
      ...User
    }
    tags {
      ...Tag
    }
    comments {
      ...Comment
    }
    likes {
      ...Like
    }
  }
}
